"use client";

// Page transitions removed — was causing snap/flash issues with Next.js app router.
// The 3D bass swimming in the background provides visual continuity between pages.
export function PageTransition() {
  return null;
}
